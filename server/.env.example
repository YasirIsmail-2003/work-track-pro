# Copy this file to .env and fill values before running in production.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM="WorkTrack Pro <no-reply@worktrack.pro>"
APP_URL=http://localhost:3000
TZ=Asia/Kolkata

# Note: Do NOT commit your real `.env` file. Keep secrets in provider environment variables (Vercel/Render/GitHub Secrets).
