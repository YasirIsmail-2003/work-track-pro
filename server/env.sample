NEXT_PUBLIC_SUPABASE_URL=https://your-supabase-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database connection for migrations (optional when using Supabase)
# Provide either DATABASE_URL (direct Postgres) or SUPABASE_DB_URL (from Supabase)
DATABASE_URL=
SUPABASE_DB_URL=

# Base URL of the frontend used in email templates
APP_URL=http://localhost:8080

# SMTP configuration for transactional emails
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=worktrack
SMTP_PASS=worktrack
SMTP_FROM="WorkTrack Pro <no-reply@worktrack.pro>"


